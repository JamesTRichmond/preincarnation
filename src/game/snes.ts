/** SNES presentation: 320×240 buffer, 16×16 metatiles.
 *  HUD is an overlay (ALttP), not a reserved strip — world uses the full frame. */
export const SNES_W = 320;
export const SNES_H = 240;
export const HUD_H = 0;
export const VIEW_H = SNES_H;
export const TILE = 16;
